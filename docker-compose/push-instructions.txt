docker login --username token?

docker push ???

docker run --name evatool evatool/evatool:latest

docker run --name evatool -e HOST_URL='evatool.org' -e FRONTEND_PORT=80 -e BACKEND_PORT=443 -e DB_PORT=3306 -e DB_PASSWORD='sqdd1432' evatool/evatool:latest

docker start evatool



TODO
- Get env vars into frontend
- Get env vars into backend
- Get mysql to start in not 5 minutes
- Get docker-compose to work
- Get docker compose to push parameterizable image to docker hub
- Deploy to UID server in both main deploy GitHub Actions
